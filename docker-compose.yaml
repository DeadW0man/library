version: '3.8'

services:
  solaris-app:
    build: .
    container_name: solaris-app
    ports:
      - "5000:5000"
    environment:
      - SOLARIS_SQLITE_PATH=/app/data/dev.db
      - SOLARIS_HOST=0.0.0.0
      - SOLARIS_PORT=5000
    volumes:

    restart: unless-stopped
